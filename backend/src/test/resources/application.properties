# --- Configurazione per i test unitari ---
# Queste proprietà vengono usate SOLO durante i test (src/test/resources)
# Utilizzano valori fittizi: non sono segreti reali

# Database in memoria H2 (non serve PostgreSQL per i test)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# JWT di test (chiave fittizia, solo per i test unitari)
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=3600000
application.security.jwt.refresh-token.expiration=604800000
